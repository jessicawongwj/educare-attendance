{
  "name": "educare-attendance",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "git push origin main && vercel deploy --prod"
  },
  "dependencies": {
    "mssql": "^11.0.1"
  }
}
